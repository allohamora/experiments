CREATE TABLE users (
  userId INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name VARCHAR(30) NOT NULL
);

CREATE TABLE posts (
  id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  content VARCHAR(100) NOT NULL,
  userId INT REFERENCES users(userId) ON UPDATE CASCADE
);

INSERT INTO users(name) 
VALUES ('john'), ('andrew'), ('alfred');

INSERT INTO posts(content, userId) 
VALUES ('john post', 1), ('andrew post', 2), ('alfred post', 3);

SELECT users.name, posts.content FROM users
--- LEFT JOIN posts USING(userId) === LEFT JOIN posts ON posts.userId = users.userId
LEFT JOIN posts USING (userId);
